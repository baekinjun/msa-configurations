server.port: 8761

spring.application:
  name: eurekaApp

eureka:
  server:
    enable-self-preservation: false
  instance:
    hostname: 14.53.156.174
    prefer-ip-address: true
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/